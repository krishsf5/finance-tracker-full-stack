{
  "name": "finance-tracker-project",
  "version": "1.0.0",
  "description": "Full-stack Finance Tracker application with React frontend and Node.js backend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run backend:dev\" \"npm run frontend:dev\"",
    "backend:dev": "cd backend && npm run dev",
    "frontend:dev": "cd frontend && npm run dev",
    "backend:start": "cd backend && npm start",
    "frontend:start": "cd frontend && npm run build && npm run preview",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "test": "echo \"Tests to be implemented\""
  },
  "keywords": [
    "finance",
    "tracker",
    "react",
    "nodejs",
    "mongodb",
    "redux"
  ],
  "author": "Finance Tracker Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

